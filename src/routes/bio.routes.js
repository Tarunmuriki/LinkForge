import {Router} from "express";import {requireAuth} from "../middleware/auth.middleware.js";import {getMe,updateMe,addSocial,updateSocial,deleteSocial,publicBio} from "../controllers/bio.controller.js";import {validate} from "../middleware/validate.middleware.js";import {bioSchema} from "../validators/schemas.js";
const r=Router();r.get("/me",requireAuth,getMe);r.put("/me",requireAuth,validate(bioSchema),updateMe);r.post("/me/social-links",requireAuth,addSocial);r.patch("/me/social-links/:id",requireAuth,updateSocial);r.delete("/me/social-links/:id",requireAuth,deleteSocial);r.get("/:username",publicBio);export default r;
