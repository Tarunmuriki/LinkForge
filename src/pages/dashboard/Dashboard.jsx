import {useQuery} from "@tanstack/react-query";import {api,unwrap} from "../../services/api";import {Card,CardHeader,CardTitle,CardContent} from "../../components/ui/Card";import Button from "../../components/ui/Button";import {Link as LinkIcon,MousePointerClick,TrendingUp,Plus,ArrowUpRight} from "lucide-react";import {useNavigate} from "react-router-dom";
export default function Dashboard(){const nav=useNavigate();const q=useQuery({queryKey:["overview"],queryFn:()=>api.get("/analytics/overview").then(unwrap)});const data=q.data||{clicks:0,links:0,devices:[],referrers:[]};const stats=[["Total links",data.links,LinkIcon],["Clicks",data.clicks.toLocaleString(),MousePointerClick],["Top device",data.devices?.[0]?data.devices[0]._id:"—",TrendingUp]];return <><div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-violet-600">Overview</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Your link engine</h1><p className="mt-1 text-sm text-zinc-500">Monitor links, clicks, and your creator page.</p></div><Button onClick={()=>nav("/links/new")}><Plus size={16}/> Create link</Button></div><div className="grid gap-4 md:grid-cols-3">{stats.map(([label,value,Icon])=><Card key={label}><CardContent className="flex items-center justify-between pt-5"><div><p className="text-sm text-zinc-500">{label}</p><p className="mt-2 text-2xl font-bold">{value}</p></div><div className="grid size-11 place-items-center rounded-xl bg-violet-50 text-violet-600"><Icon size={20}/></div></CardContent></Card>)}</div><div className="mt-5 grid gap-5 lg:grid-cols-2"><Card><CardHeader><CardTitle>Top referrers</CardTitle></CardHeader><CardContent><div className="space-y-4">{(data.referrers||[]).map(x=><div key={x._id} className="flex items-center justify-between"><span className="text-sm">{x._id}</span><span className="text-sm font-semibold">{x.count.toLocaleString()}</span></div>)}</div></CardContent></Card><Card><CardHeader><CardTitle>Build your public page</CardTitle></CardHeader><CardContent><p className="text-sm leading-6 text-zinc-500">Customize your avatar, bio, social links, and theme, then share one public URL.</p><Button variant="outline" className="mt-4" onClick={()=>nav("/bio/customize")}>Customize Bio <ArrowUpRight size={16}/></Button></CardContent></Card></div></>}
