import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import Button from "../../components/ui/Button";import Input from "../../components/ui/Input";import {api} from "../../services/api";
export default function Signup(){const [f,setF]=useState({name:"",email:"",username:"",password:""}),[error,setError]=useState("");const nav=useNavigate();const set=k=>e=>setF({...f,[k]:e.target.value});async function submit(e){e.preventDefault();try{await api.post("/auth/register",f);nav("/dashboard")}catch(e){setError(e.response?.data?.error?.message||"Signup failed")}}return <div className="grid min-h-screen place-items-center bg-zinc-950 p-5"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><div className="mb-7 text-center"><div className="mx-auto mb-4 grid size-11 place-items-center rounded-xl bg-violet-600 text-white">◈</div><h1 className="text-2xl font-bold">Create your account</h1><p className="mt-1 text-sm text-zinc-500">Start managing links and your public bio.</p></div>{error&&<div className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-600">{error}</div>}{[["name","Full name","text"],["email","Email","email"],["username","Username","text"],["password","Password","password"]].map(([k,l,t])=><div key={k} className="mb-4"><label className="mb-2 block text-sm font-medium">{l}</label><Input type={t} value={f[k]} onChange={set(k)} placeholder={k==="username"?"tarun":""}/></div>)}<Button size="lg" className="w-full">Create account</Button><p className="mt-5 text-center text-sm text-zinc-500">Already have an account? <Link className="text-violet-600" to="/login">Sign in</Link></p></form></div>}
