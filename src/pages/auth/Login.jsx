import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import Button from "../../components/ui/Button";import Input from "../../components/ui/Input";import {api} from "../../services/api";
export default function Login(){const [email,setEmail]=useState("demo@linkforge.dev"),[password,setPassword]=useState("password123"),[error,setError]=useState("");const nav=useNavigate();async function submit(e){e.preventDefault();try{await api.post("/auth/login",{email,password});nav("/dashboard")}catch(e){setError(e.response?.data?.error?.message||"Login failed")}}return <div className="grid min-h-screen place-items-center bg-zinc-950 p-5"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><div className="mb-8 text-center"><div className="mx-auto mb-4 grid size-11 place-items-center rounded-xl bg-violet-600 text-white">◈</div><h1 className="text-2xl font-bold">Welcome back</h1><p className="mt-1 text-sm text-zinc-500">Sign in to your LinkForge workspace.</p></div>{error&&<div className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-600">{error}</div>}<label className="mb-2 block text-sm font-medium">Email</label><Input value={email} onChange={e=>setEmail(e.target.value)} type="email"/><label className="mb-2 mt-4 block text-sm font-medium">Password</label><Input value={password} onChange={e=>setPassword(e.target.value)} type="password"/><Button size="lg" className="mt-6 w-full">Sign in</Button><p className="mt-5 text-center text-sm text-zinc-500">New here? <Link className="font-medium text-violet-600" to="/signup">Create account</Link></p></form></div>}
