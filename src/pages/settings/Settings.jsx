import {useAuth} from "../../hooks/useAuth";import {Card,CardHeader,CardTitle,CardContent} from "../../components/ui/Card";import Input from "../../components/ui/Input";
export default function Settings(){const {user}=useAuth();return <><div className="mb-7"><h1 className="text-3xl font-bold">Settings</h1><p className="mt-1 text-sm text-zinc-500">Manage your workspace profile and security.</p></div><Card className="max-w-2xl"><CardHeader><CardTitle>Profile</CardTitle></CardHeader><CardContent><label className="mb-2 block text-sm font-medium">Name</label><Input value={user?.name||""} readOnly/><label className="mb-2 mt-4 block text-sm font-medium">Email</label><Input value={user?.email||""} readOnly/><label className="mb-2 mt-4 block text-sm font-medium">Username</label><Input value={user?.username||""} readOnly/></CardContent></Card></>}
